# itl

Rust terminal tooling for traversing an iTunes/Music Library export.

## Dev checks

- Format: `cargo fmt --all -- --check`
- Lint: `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- Test: `cargo test --workspace --all-features`

Run the stub binary:

`cargo run -p itl-cli`
